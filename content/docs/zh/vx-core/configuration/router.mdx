---
title: 路由
description: RouterConfig 与 RuleConfig
---

路由在 `ServerConfig.router`（`RouterConfig`）中配置。模式定义：[`protos/proto/vx/router/router.proto`](https://github.com/5vnetwork/vx-core/blob/main/protos/proto/vx/router/router.proto)。

规则按顺序评估，第一条匹配的规则决定出站或选择器。命中后后续规则不在考虑。


## 示例

全部路由到直连：

```json
{
  "router": {
    "rules": [
      { "matchAll": true, "outboundTag": "direct" }
    ]
  }
}
```

将某入站路由到某出站：

```json
{
  "router": {
    "rules": [
      {
        "inboundTags": ["trojan"],
        "matchAll": true,
        "outboundTag": "direct"
      }
    ]
  }
}
```

## RouterConfig

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `rules` | `RuleConfig[]` | 有序路由规则 |

## RuleConfig

**目标：** `outboundTag` 与 `selectorTag` 须恰好设置其一。若均为空，连接将被**阻断**。

| 字段 | 说明 |
| --- | --- |
| `outboundTag` | 发往该出站处理器 |
| `selectorTag` | 发往[选择器](#selectorconfig)（客户端 / 负载均衡） |
| `condition` | 用于匹配连接的Metadata |
| `fallbacks` | 主目标失败时尝试备用选择器/出站 |
| `ruleName` | 名称 |

### Fallback

| 字段 | 说明 |
| --- | --- |
| `selectorTag`、`outboundTag` | 备用目标 |
| `action.ipToDomain` | 回退时将 IP 改写回域名 |
| `condition` | 本回退的条件，成立时回退才会生效 |
| `last` | 若条件匹配，此回退为最终回退，后面的不在考虑 |

## Condition

用于 `rule.condition` 或 `fallback.condition`。

| 字段 | 说明 |
| --- | --- |
| `matchAll` | 匹配所有连接 |
| `usernames` | 匹配已认证用户 id |
| `srcCidrs`、`srcIpTags` | 源 IP 匹配 |
| `dstCidrs`、`dstIpTags` | 目标 IP 匹配 |
| `geoDomains`、`domainTags` | 域名匹配（使用 [geo](/zh/docs/vx-core/configuration/geo) 集合） |
| `networks` | `TCP`、`UDP` 等 |
| `srcPortRanges`、`dstPortRanges` | 端口范围 |
| `appIds`、`appTags` | 应用 id 匹配（客户端） |
| `protocols` | 协议嗅探结果 |
| `ipv6` | 仅匹配 IPv6 |
| `fakeIp` | 匹配 Fake IP 目标 |
| `resolveDomain` | 解析域名；所有 IP 须在 `dstIpTags` 或 `dstCidrs` 中 |
| `resolveSoftRewrite` | 解析域名，任意IP在`dstIpTags` 或 `dstCidrs` 中即可，并将目标改写为此 IP |
| `resolveSoftNoRewrite` | 解析域名，任意IP在`dstIpTags` 或 `dstCidrs` 中即可，但不改写目标 |
| `skipSniff` | 对 IP 目标连接禁用嗅探 |
| `inboundTags` | 匹配入站 `tag` |

## SelectorConfig

选择器按标签过滤与选取策略分组出站（主要用于**客户端**）。

| 字段 | 说明 |
| --- | --- |
| `tag` | 规则中 `selectorTag` 引用 |
| `filter` | `prefixes`、`tags`、`subStrings`、`inverse` |
| `strategy` | `ALL`、`ALL_OK`、`LEAST_PING`、`MOST_THROUGHPUT`、`TOP_PING`、`TOP_THROUGHPUT` |
| `balanceStrategy` | `RANDOM` 或 `MEMORY` |
| `selectFromOm` | 从出站管理器选择而非数据库，由于服务端并不支持数据库，服务端若使用选择器，此应该为true |
| `speedTestSize`、`speedTestInterval`、`pingTestInterval` 等 | 健康检查与测速 |


## 相关

- [Geo](/zh/docs/vx-core/configuration/geo) — `domainTags`、`dstIpTags`、`geoDomains`
- [DNS](/zh/docs/vx-core/configuration/dns) — `resolveDomain` 规则的解析
- [调度器](/zh/docs/vx-core/configuration/dispatcher) — 嗅探影响协议/域名匹配
