---
title: Socket
description: SocketConfig
---

Proto: [`protos/proto/vx/transport/dlhelper.proto`](https://github.com/5vnetwork/vx-core/blob/main/protos/proto/vx/transport/dlhelper.proto)

`SocketConfig` is set on `TransportConfig.socket` and on `MultiProxyInboundConfig.socket`.

## SocketConfig

<ProtoField name="mark">

`SO_MARK` (non-zero)

</ProtoField>

<ProtoField name="tfo">

TCP fast open: `AsIs`, `Enable`, `Disable`

</ProtoField>

<ProtoField name="tproxy">

`Off`, `TProxy`, `Redirect`

</ProtoField>

<ProtoField name="receiveOriginalDestAddress">

UDP: receive original destination (IP_RECVORIGDSTADDR)

</ProtoField>

<ProtoField name="bindAddress, bindPort">

Bind local address (Linux)

</ProtoField>


<ProtoField name="acceptProxyProtocol">

Accept PROXY protocol v1/v2

</ProtoField>

<ProtoField name="tcpKeepAliveInterval, tcpKeepAliveIdle">

TCP keepalive

</ProtoField>


<ProtoField name="tfoQueueLength">

TFO queue length

</ProtoField>

<ProtoField name="bindToDevice">

Bind to network interface index

</ProtoField>

<ProtoField name="rxBufSize, txBufSize, forceBufSize">

Socket buffer sizes

</ProtoField>

<ProtoField name="localAddr4, localAddr6">

Outbound local address (dial / UDP listen)

</ProtoField>


<ProtoField name="dialTimeout">

Dial timeout override

</ProtoField>

```json
{
  "transport": {
    "socket": {
      "tfo": "Enable",
      "tcpKeepAliveIdle": 60
    },
    "tcp": {}
  }
}
```
